function List () {};

List.prototype.add = function (value) {
    return value;
};

List.prototype.getAt = function(index) {
    return index;
};

List.prototype.removeAt = function (index) {
    return index;
};

List.prototype.indexOf = function(index) {
    return index;
};

module.exports = List;